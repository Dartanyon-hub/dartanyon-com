# dartanyon.com

Personal landing page. One HTML file, no build, no dependencies.

Deployed via Cloudflare Pages.

## Local preview

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
